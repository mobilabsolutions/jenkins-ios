language: objective-c
xcode_project: JenkinsiOS.xcodeproj
osx_image: xcode9.4
xcode_sdk: iphonesimulator11.4

cache:
- bundler

before_install:
- bundle install

branches:
  only:
    - master
    - /^(\d+\.)?(\d+\.)?\d+(-\w+)?$/

script:
- ./travis-build.sh
